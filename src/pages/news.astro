---
import { getCollection } from 'astro:content';
const posts = await getCollection('news');
---

<h1>News</h1>
<ul>
  {posts.map(post => (
    <li><a href={`/news/${post.slug}/`}>{post.data.title}</a> - {post.data.date}</li>
  ))}
</ul>
